name: py-escalation
services:
  - name: gateway
    source: gateway
    database: gateway
    language: python
    role: gateway
    aliases: ["gateway", "localhost:8080"]
  - name: users-svc
    source: usersvc
    database: usersvc
    language: python
    role: internal
    aliases: ["users-svc", "localhost:9000"]
